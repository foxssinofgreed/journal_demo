extends ../layout

block content
    .container
        h1.mb-1 #{article.title}
        .text-muted #{article.createdAt.toLocaleDateString()}

        a(href='/article').btn.btn-secondary All Articles
        a(href='/article/edit/#{article.slug}').btn.btn-info Edit

        !{article.sanitizedHtml}